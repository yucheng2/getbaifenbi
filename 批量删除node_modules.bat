@echo off
set nowPath=%cd%
cd \ 
cd %nowPath%
::delete specify file(*.pdb, *.vshost. =*)
@REM for /r %nowPath% %%i in (*.pab, *.vshost.*) do (del %%i)
::delete apecify folder(obj, bin)
@REM for /r %nowPath% %%i in (obj, bin) do (if exist %%i rd /s /q %%i)
for /r %nowPath% %%i in (node_modules) do (if exist %%i rd /s /q %%i)
@rem echo ok
@rem pause